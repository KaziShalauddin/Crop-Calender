@model IEnumerable<CropCalender.Models.EntityModels.CropVariety>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Crop.CropName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CropVarietiesName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CropVarietiesDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Crop.CropName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CropVarietiesName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CropVarietiesDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
